#INSTUCTIONS

```bash

create a folder django basics(mkdir django basics)
Inside a django basics directory run this command* (virtualenv -p python3 .)
Then create a src folder create a project by running this command(django-admin startproject djang)Note:djang is my project name
Inside a src folder you can start buiding app framework by running this command(django admin startapp modelname) to start building models
Once you done building model execute manage.py with this command (python manage.py makemigrations and python manage.py migrate)

for further info use the django docs to further develop your project

!!HAVE A GREAT DAY

```